###############################################################
#	Global application values
###############################################################
from enum import Enum as ENUM

class Data(ENUM):
        CHANNELS = "channels"
        SERIES = "sinfo" 
        EPISODES = "einfo"